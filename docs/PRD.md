# 「轻生活」产品需求文档（PRD）

| 项目信息 | 内容 |
| --- | --- |
| 文档语言 | 简体中文 |
| 产品名称 | 轻生活（项目代号 `qingshenghuo`） |
| 产品形态 | 移动端优先的响应式 Web / PWA（生活化减肥工具） |
| 建议技术栈 | React 18 + TypeScript + Vite + Tailwind CSS + PWA；后端 Node.js(NestJS) 或 Next.js 全栈；PostgreSQL（MVP 可用 SQLite）；图表 ECharts；部署 Docker Compose |
| 文档类型 | 简单 PRD（如需竞品分析请另行提出） |
| 版本 | v1.0（正式化首版） |
| 核心理念 | 不节食、不极端，把减肥融入日常生活 |

## 原始需求复述

为 20–45 岁、无专业营养知识、讨厌复杂操作与极端节食的普通上班族/学生，提供一款中文减肥网站。通过**科学卡路里计算**、**低门槛饮食记录（3 次点击记一餐）**、**习惯养成**与**数据可视化**，帮助用户可持续健康减重。差异化定位：以中式食物/外卖/奶茶零食为一等公民；明确采用「无负罪感设计」，无断签焦虑；微量营养素面板免费（竞品多为付费墙）。

---

## 1. 产品目标与成功指标

### 1.1 产品目标（3 个正交目标）

| # | 目标 | 说明 |
| --- | --- | --- |
| G1 | **记一餐足够便宜** | 把单餐记录的交互成本降到 ≤3 次点击/≤15 秒，让「记录」不再是负担 |
| G2 | **数字可信且安全** | 热量指标基于可溯源公式，强制安全下限与缺口上限，绝不给出危险建议 |
| G3 | **坚持不需要意志力** | 无负罪感文案与设计，断签无惩罚，用趋势而非单日波动评价用户 |

### 1.2 成功指标（MVP 上线后 4 周观测）

| 指标 | 目标值 | 衡量方式 |
| --- | --- | --- |
| 首次引导完成率 | ≥ 70% | 完成 `/onboarding` 问卷 / 进入引导页 |
| 新用户次日留存 | ≥ 35% | 第 2 日有登录或记录行为 |
| 单餐记录平均点击次数 | ≤ 3 | 埋点统计记录流程点击事件 |
| 记录接口 P95 响应 | < 300ms | 服务端监控 |
| 首屏可交互时间 | < 2s | 移动端 4G 环境 |
| 热量引擎单元测试通过率 | 100% | CI 强制门禁 |
| 7 日内 ≥3 天有记录的用户占比 | ≥ 40% | 埋点 |

> 说明：埋点仅使用**自托管的第一方统计**，**不得接入任何第三方行为分析 SDK**（见 NFR-7 与隐私承诺）。

---

## 2. 目标用户与使用场景

### 2.1 目标用户

- **主画像**：20–45 岁上班族/学生，BMI 偏高或希望减脂，无营养学背景。
- **次要画像**：孕期/哺乳期或慢病人群（**不作为减重目标人群**，仅需被安全提示拦截）。

### 2.2 核心场景

| # | 场景 | 用户诉求 |
| --- | --- | --- |
| S1 | 午餐点麻辣烫，边吃边记 | 不想搜十几样配料，只想快速估个热量并看到今天还剩多少 |
| S2 | 下午想喝一杯奶茶 | 想知道这杯多少卡，以及有没有更低卡的点法 |
| S3 | 晚上称重比昨天重了 0.8kg | 不要被红色警告吓到，想看趋势而非单日数字 |
| S4 | 出差一周没记录，回来想重新开始 | 不想看到「连续打卡 0 天」，不想被追责 |
| S5 | 想验证这个网站靠不靠谱 | 想知道热量公式出处，能否免注册先算一次 |
| S6 | 想换手机/退出 | 想把自己的全部数据导出带走并彻底删除 |

---

## 3. 用户故事（按角色分组，含验收标准）

### 3.1 未注册访客

- **US-01**：As a 访客，I want 在落地页免注册试用热量计算器，so that 我先确认产品有用再注册。
  - **验收**：`/` 页输入性别/年龄/身高/体重/活动量后立即返回 BMR、TDEE、建议摄入，无需登录；返回结果附「参考来源」入口与免责声明。

- **US-02**：As a 访客，I want 在落地页看到隐私承诺，so that 我放心地把体重数据交出去。
  - **验收**：`/` 页含「不出售数据、无广告 SDK、无第三方行为分析」文案与免责声明；不提供「一键登录即同意全部营销」类捆绑选项。

### 3.2 新用户

- **US-03**：As a 新用户，I want 用邮箱+验证码完成注册与引导，so that 快速拿到我的每日热量预算。
  - **验收**：注册支持邮箱验证码登录；`/onboarding` 收集性别、出生日期、身高、当前体重、目标体重、目标期限、活动量、饮食偏好、常见疾病；完成后跳转 `/dashboard` 并展示建议摄入。

- **US-04**：As a 新用户，I want 在引导前被告知适用边界，so that 我不会误用不适合我的方案。
  - **验收**：引导首屏展示**免责声明**（不提供医疗建议；孕期/哺乳期/疾病治疗期不建议使用热量缺口方案），需主动确认后方可继续。

- **US-05**：As a 新用户，I want 修改我的基础数据后系统自动重算，so that 我的预算始终正确。
  - **验收**：在 `/profile` 修改性别/身高/体重/目标/活动量后，BMR、TDEE、每日缺口、建议摄入、宏量营养素目标全部自动重算并即时生效。

### 3.3 记录型用户

- **US-06**：As a 上班族，I want 用「搜索 + 常见份量」在 3 次点击内记完一餐，so that 记录不打断我的生活。
  - **验收**：从 `/diary` 起，点击「+ 记录」→ 选定食物 → 选择份量并确认 = 3 次点击；记录后立刻反馈本餐热量与今日进度环。

- **US-07**：As a 用户，I want 对没有收录的食物快速加卡，so that 我不会因为搜不到就不记。
  - **验收**：快速加卡支持仅输入「名称 + 热量」即可入库，不强制填写三大营养素。

- **US-08**：As a 用户，I want 扫码录入包装食品，so that 不用手动查营养表。
  - **验收**：扫描条形码命中 Open Food Facts 时返回每 100g 营养并写入当日记录；未命中给出「未收录，是否快速加卡」引导。

- **US-09**：As a 用户，I want 用 AI 拍照/描述识别食物，so that 更省事。
  - **验收**：AI 仅返回**食物名称与数量**；热量数值必须由食物库匹配得出；AI 结果填入表单后**必须用户确认才入库**；AI 未配置时该入口隐藏或提示不可用，其余功能不受影响。

- **US-10**：As a 常吃固定早餐的用户，I want 把一餐存为套餐模板，so that 一键复用。
  - **验收**：可将当前餐次保存为模板并一键添加；模板修改不影响已生成的历史记录。

### 3.4 外卖 / 社交场景用户

- **US-11**：As a 用户，I want 用外卖换算器估算麻辣烫/黄焖鸡/轻食沙拉，so that 我不用逐样搜索。
  - **验收**：`/tools` 选择品类与份量档位后输出热量估算区间与「这样点更健康」替换建议。

- **US-12**：As a 用户，I want 知道一包薯片等于多少运动，so that 我建立直观认知但不被恐吓。
  - **验收**：零食救赎同时给出低卡替代与「吃掉它需要多少运动」换算（如「一包薯片 ≈ 快走 70 分钟」），文案不出现恐吓式措辞。

- **US-13**：As a 用户，I want 聚餐时一键估算并调整当天其他餐，so that 聚餐不再让我崩溃。
  - **验收**：聚餐模式支持火锅/烧烤/自助餐估算，并给出当天其余餐次的调整建议；不产生「超标警告」红色文案。

### 3.5 追踪与习惯用户

- **US-14**：As a 用户，I want 记录体重并看到带 7 日移动平均的趋势线，so that 我不被单日波动影响情绪。
  - **验收**：`/weight` 支持每日体重记录与备注；趋势图叠加 7 日移动平均线；体重上涨时展示「波动很正常，看趋势就好」。

- **US-15**：As a 用户，I want 运动消耗计入当日热量平衡，so that 我的预算更真实。
  - **验收**：选择运动项目与时长后，按 `消耗 = MET × 体重kg × 时长h` 计算并计入当日余额。

- **US-16**：As a 用户，I want 一键记录饮水，so that 打卡最简单的事也能坚持。
  - **验收**：首页水杯组件一键 +250ml；支持撤销上一条；可自定义每日目标。

- **US-17**：As a 用户，I want 断签后不被惩罚、被温柔召回，so that 我随时可以回来。
  - **验收**：习惯打卡展示当前连续/历史最佳；断签不发惩罚性提醒，文案为「休息一下没关系，随时回来」。

### 3.6 数据主权用户

- **US-18**：As a 用户，I want 一键导出全部数据为 JSON + CSV，so that 数据真正属于我。
  - **验收**：`/settings/data` 可导出 JSON（完整可恢复备份）与 CSV（模板见 §9.5）；导出包含体重、饮食、运动、习惯、饮水、设置。

- **US-19**：As a 用户，I want 导入历史数据、彻底删除账号，so that 我能自主掌控。
  - **验收**：支持 JSON 备份恢复与 CSV 模板批量导入历史体重；账号删除为**硬删除**（数据库真实移除，非标记删除）。

### 3.7 安全敏感用户

- **US-20**：As a 有慢病/孕产情况的用户，I want 被明确提示本产品不适用，so that 我不被误导。
  - **验收**：引导问卷中勾选孕期/哺乳期/疾病治疗期时，展示醒目安全提示并建议咨询专业医师；不提供「继续使用减重方案」的一键按钮。

- **US-21**：As a 使用 AI 助手的用户，I want 在被检测到高风险意图时得到固定就医建议，so that AI 不越界。
  - **验收**：检测到疾病诊断、停药、极端节食类意图时，固定回复就医建议，不给出具体医疗/用药意见。

---

## 4. 需求池（P0 / P1 / P2）

优先级定义：**P0 = 必须有**（MVP 阻塞），**P1 = 应该有**，**P2 = 可以有**。

### 4.1 用户系统与首次引导

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R1.1 | 邮箱 + 验证码注册/登录 | P0 | 一期 |
| R1.2 | 微信扫码登录（可选） | P2 | 三期 |
| R1.3 | 引导问卷：性别/出生日期/身高/当前体重/目标体重/目标期限/活动量/饮食偏好/常见疾病 | P0 | 一期 |
| R1.4 | 基础数据可修改，修改后自动重算全部热量指标 | P0 | 一期 |
| R1.5 | 强制免责声明（不提供医疗建议；孕期/哺乳期/疾病治疗期不建议使用热量缺口方案） | P0 | 一期 |
| R1.6 | 密码 bcrypt 存储 | P0 | 一期 |
| R1.7 | JWT 鉴权；服务端一律从 JWT 取 userId | P0 | 一期 |

### 4.2 热量计算引擎（核心）

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R2.1 | BMR 采用 Mifflin-St Jeor 公式 | P0 | 一期 |
| R2.2 | TDEE = BMR × 活动系数（久坐 1.2 / 轻度 1.375 / 中度 1.55 / 高强度 1.725 / 运动员 1.9） | P0 | 一期 |
| R2.3 | 每日缺口 = 每周目标减重kg × 7700 ÷ 7 | P0 | 一期 |
| R2.4 | 缺口不得超过 TDEE 的 30%（超出强制截断并提示） | P0 | 一期 |
| R2.5 | 安全下限：女 1200 / 男 1500 kcal；触下限给醒目安全提示 | P0 | 一期 |
| R2.6 | 目标达成预测曲线 vs 实际体重曲线折线图 | P0 | 一期 |
| R2.7 | 接近目标时逐步收窄缺口（taper，防反弹） | P1 | 二期 |
| R2.8 | 宏量营养素默认 蛋白 25% / 脂肪 25% / 碳水 50%，按 4/9/4 kcal/g 换算，可自定义 | P0 | 一期 |
| R2.9 | 站内「参考来源」页列出公式与数据来源（Mifflin-St Jeor 1990、WHO BMI 标准、2024 成人活动 MET 汇编），数字可溯源 | P0 | 一期 |
| R2.10 | 热量引擎完整单元测试，覆盖全部公式与边界 | P0 | 一期 |
| R2.11 | kcal / kJ 全局切换 | P1 | 二期 |

### 4.3 饮食记录

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R3.1 | 内置中式食物库 ≥500 条（家常菜/主食/外卖常见菜/奶茶/零食/水果） | P0 | 一期 |
| R3.2 | 食物字段：每 100g 热量、蛋白/脂肪/碳水、常见份量描述（一碗/一个/一杯/一片） | P0 | 一期 |
| R3.3 | 自然份量输入（个/碗/杯/片/袋），每单位可自定义克数 | P0 | 一期 |
| R3.4 | 记录方式①：搜索/分类浏览（模糊搜索、收藏、最近吃过一键复记） | P0 | 一期 |
| R3.5 | 记录方式②：快速加卡（跳过搜索，输名称 + 热量） | P0 | 一期 |
| R3.6 | 记录方式③：条形码扫描（Open Food Facts） | P1 | 二期 |
| R3.7 | 记录方式④：AI 识别（仅识别名称与数量，热量必须由食物库匹配；结果需用户确认） | P2 | 三期 |
| R3.8 | 按餐组织（早/午/晚/加餐，每餐独立热量预算，支持餐间拖拽） | P0 | 一期 |
| R3.9 | 套餐模板（常吃一餐存为组合，一键添加） | P0 | 一期 |
| R3.10 | 记录后实时反馈（本餐热量 + 今日进度环） | P0 | 一期 |

### 4.4 微量营养素面板

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R4.1 | 日/周视图追踪 10 项：膳食纤维、钠、饱和脂肪、糖、钙、铁、钾、维生素 D、B12、镁 | P1 | 二期 |
| R4.2 | 可选 DRI 参考条 | P1 | 二期 |
| R4.3 | 作为免费差异化卖点，不做付费墙 | P1 | 二期 |

### 4.5 生活化特色功能

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R5.1 | 外卖换算器（麻辣烫/黄焖鸡/轻食沙拉 → 热量估算 + 替换建议） | P1 | 二期 |
| R5.2 | 零食救赎（低卡替代 + 「吃掉它需要多少运动」换算） | P1 | 二期 |
| R5.3 | 聚餐模式（火锅/烧烤/自助餐估算 + 当天其他餐调整建议） | P1 | 二期 |
| R5.4 | 饮品计算器（奶茶/咖啡/酒类 + 低卡点单攻略） | P1 | 二期 |

### 4.6 运动与饮水

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R6.1 | 常见运动 MET 表（走路/跑步/游泳/骑行/跳绳/爬楼/家务） | P1 | 二期 |
| R6.2 | 消耗 = MET × 体重 × 时长，计入当日热量平衡 | P1 | 二期 |
| R6.3 | 饮水追踪（首页水杯组件，一键 +250ml，可撤销上一条，自定义目标） | P1 | 二期 |

### 4.7 体重与习惯追踪

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R7.1 | 每日体重记录（建议早晨空腹，支持备注） | P0 | 一期 |
| R7.2 | 趋势图带 7 日移动平均线 | P0 | 一期 |
| R7.3 | 习惯打卡（喝水/早睡/步数/不点外卖等，可自定义） | P1 | 二期 |
| R7.4 | 无负罪感 streak（当前连续/历史最佳；断签不惩罚；「休息一下没关系，随时回来」） | P1 | 二期 |

### 4.8 间歇性断食计时器

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R8.1 | 16:8 / 18:6 计时器，首页显示剩余时间，默认关闭 | P2 | 二期 |
| R8.2 | 首次开启必须弹内容警告；提供饮食障碍求助资源链接；「这不适合我」为一级按钮 | P1 | 二期 |
| R8.3 | 不做任何主动推送怂恿断食 | P1 | 二期 |

### 4.9 AI 助手

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R9.1 | 每日总结（晚饭后生成当日复盘：直接结论 + 依据 + 一条可执行建议） | P2 | 三期 |
| R9.2 | 今日方案（据前两日记录生成建议；数据未变则复用缓存） | P2 | 三期 |
| R9.3 | 自由提问（如「我今天还能吃 X 吗」） | P2 | 三期 |
| R9.4 | 限额每账号每日 50 次 | P2 | 三期 |
| R9.5 | AI key 只能存服务端环境变量 | P0 | 三期 |
| R9.6 | 安全边界：不做疾病诊断、不建议停药、不鼓励极端节食；命中相关意图固定回复就医建议 | P0 | 三期 |

### 4.10 数据主权与隐私

| ID | 需求 | 优先级 | 分期 |
| --- | --- | --- | --- |
| R10.1 | 一键导出全部数据为 JSON + CSV | P1 | 二期 |
| R10.2 | JSON 备份恢复 | P1 | 二期 |
| R10.3 | CSV 模板批量导入历史体重 | P1 | 二期 |
| R10.4 | 账号删除（硬删除） | P1 | 二期 |
| R10.5 | 隐私承诺写入落地页（不出售数据、无广告 SDK、无第三方行为分析） | P0 | 一期 |

### 4.11 非功能性需求

| ID | 需求 | 优先级 |
| --- | --- | --- |
| NFR-1 | 密码 bcrypt 加密存储 | P0 |
| NFR-2 | JWT 鉴权；服务端接口一律从 JWT 取用户 ID，**禁止客户端指定他人 ID** | P0 |
| NFR-3 | 首屏 < 2s；记录接口 < 300ms | P0 |
| NFR-4 | 食物库本地缓存；PWA 离线可查看历史与记录 | P1 |
| NFR-5 | 单元测试覆盖热量引擎全部公式与边界（极低体重、大年龄、下限触发、30% 缺口上限） | P0 |
| NFR-6 | AI key 仅允许出现在服务端环境变量；未配置时其余功能不受影响并给出明确提示 | P0 |
| NFR-7 | 无障碍 WCAG AA 对比度；核心流程支持屏幕阅读器 | P1 |
| NFR-8 | 深色模式 | P1 |
| NFR-9 | 移动端优先响应式；核心记录操作 ≤3 次点击 | P0 |
| NFR-10 | 不主动发用户未开启的通知 | P0 |
| NFR-11 | **可解释性闭环**：任何新增的**用户可见判定或数字**必须在 `/why-numbers` 同步提供来源与算法说明（R11.1 / R11.2 首个适用对象） | P1 |

### 4.12 自用场景增强（四期新增）

> **来源**：`docs/PRD-增量需求候选.md` 中用户选定的 **N1（平台期说明）** 与 **N4（目标达成后的维持模式）**，
> 经确认后转为正式需求。二者面向**单人长期使用**场景，分别解决两个真实断点：
> ① 体重卡住时不想放弃；② 减到目标之后怎么办。
> **本节为新增**：原 R1.x~R10.x 共 56 条需求未作任何改动。

#### 4.12.1 需求登记

| ID | 需求 | 优先级 | 分期 | 验收标准 |
| --- | --- | --- | --- | --- |
| R11.1 | **平台期说明**：体重连续 3 周无显著变化时，主动解释常见原因并提供 4 周斜率视角 | P0 | 四期 | AC-11.1.1~11.1.7（见 §4.12.2） |
| R11.2 | **目标达成后的维持模式**：体重达到目标后缺口归零、摄入回到 TDEE，UI 转为维持态；含目标达成进度环与「还需 X 周」的维持态呈现 | P1 | 四期 | AC-11.2.1~11.2.7（见 §4.12.3） |

#### 4.12.2 R11.1 平台期说明

**需求说明**：识别平台期并在 `/weight` 展示说明卡片，内容为「解释常见原因 + 4 周移动平均斜率视角」。
平台期判定与斜率计算必须为 `@qsh/core` 纯函数（零 IO，时间由参数注入）。

| ID | 验收标准（可测） | 优先级 |
| --- | --- | --- |
| AC-11.1.1 | **判定规则**：以 7 日移动平均序列为输入，当最近**连续 21 天**的移动平均变化**绝对值 < 0.1 kg** 时判定进入平台期；判定阈值与计算过程必须在 `/why-numbers` 可查（默认取值见 Q15） | P0 |
| AC-11.1.2 | **触发前置条件**：仅当 ① 体重记录 ≥ 21 天 且 ② 未处于维持模式（R11.2）时展示；记录不足 21 天时**不判定、不展示、不报错** | P0 |
| AC-11.1.3 | **内容完整性**：卡片必须**同时**包含 ① 平台期常见原因的说明（水分波动 / 记录精度 / 身体适应，需给出实质解释，不得只用「这很正常」一笔带过）② 4 周（28 天）移动平均斜率视图；缺任一即为不通过 | P0 |
| AC-11.1.4 | **文案红线（可自动化校验）**：文案中**不得**出现「失败」「停滞」「退步」「没有效果」「无效」「努力白费」「警告」等评判性或警示性词语；**不得**使用红色作为警示语义（沿用 §7 语气规范与项目内文案 grep 校验） | P0 |
| AC-11.1.5 | **建议动作白名单（硬性）**：卡片允许给出的可执行动作**仅限** ① 检查记录完整性（如「看看这几天的记录是否完整」）② 放宽目标期限；出现「减少摄入」「加大缺口」「吃得更少」「增加运动量」等任一表述即判定**不通过** | P0 |
| AC-11.1.6 | **展示频控**：同一天内最多展示一次；用户手动关闭后 **7 天内不再自动出现**；关闭状态持久化（存本地即可，不强制服务端） | P1 |
| AC-11.1.7 | **纯函数与单测**：平台期判定与 4 周斜率计算实现为 `@qsh/core` 纯函数，并补单测覆盖：无平台期 / 恰好 21 天 / 数据中含缺失日 / 记录不足 21 天（不判定）/ 体重回升后退出平台期 | P0 |

#### 4.12.3 R11.2 目标达成后的维持模式

**需求说明**：体重达到目标体重后进入维持模式 —— **不再制造热量缺口，建议摄入回到 TDEE**。
同时打包处理：目标达成进度环与「还需 X 周」在 `etaWeeks = null`（已达目标）时的维持态呈现。

| ID | 验收标准（可测） | 优先级 |
| --- | --- | --- |
| AC-11.2.1 | **引擎状态判定**：当 `weightKg <= targetWeightKg`（容差 **±0.2 kg**，避免日波动导致状态抖动）时进入维持模式：`effectiveDeficit = 0`、`intakeRecommended = round(tdeeFloat)`、`isDeficitCapped = false`、`floorApplied = false`；**不得抛异常** | P0 |
| AC-11.2.2 | **缺口归零（核心）**：维持模式下**不得**产生任何热量缺口；`intakeRecommended` 必须与 `tdee` 相等（±1 kcal 取整容差）——即**摄入回到 TDEE** | P0 |
| AC-11.2.3 | **不误触发安全提示**：维持模式下**不得**出现安全下限提示与 30% 缺口上限提示；`safetyMessages` 不含这两类文案 | P0 |
| AC-11.2.4 | **进度环与 ETA 维持态（打包项）**：`etaWeeks` 在已达目标时为 `null`，该**空态必须被维持模式接管**——进度环显示为 100%（达成态）；原「还需 X 周」区域**不得留空、不得显示 0 周或「—」**，必须替换为维持态文案（如「已经达成，保持在 X kg 附近就很好」） | P0 |
| AC-11.2.5 | **文案红线（可自动化校验）**：维持态文案**不得**包含「继续减重」「再定个新目标」「加大缺口」「还可以更瘦」「挑战更低体重」等暗示；**不得**使用红色警示语义 | P0 |
| AC-11.2.6 | **不主动推荐新目标（硬性）**：可提供「设定新目标」入口，但**必须由用户主动触发**；系统不得自动推荐新目标、不得主动推送（遵守 NFR-10）。**默认状态为停留在维持模式** | P0 |
| AC-11.2.7 | **可逆且不丢历史**：用户修改目标体重后自动退出维持模式并重算（沿用 R1.4）；目标变更历史继续追加至 `weight_goal_history`，**不得覆盖**现有记录。纯函数单测需覆盖：未达目标（不进入）/ 恰好达成（进入）/ 达成后回升 0.1kg（容差内**不退出**）/ 回升 0.5kg（退出）/ 目标体重改小后退出 | P0 |

#### 4.12.4 约束与风险（沿用候选文档，作为硬性约束）

- **可解释性（硬约束）**：R11.1 与 R11.2 均会产生新的用户可见判定 / 数字（平台期阈值、4 周斜率、维持态摄入），
  **必须在 `/why-numbers` 同步新增条目**说明来源与算法（见 NFR-11）。
- **R11.1 风险 —— 滑向极端节食**：平台期极易被误设计成「那就吃更少 / 动更多」的处方。
  **规避**：可执行动作仅限 AC-11.1.5 白名单的两类，任何形式的「加大缺口」表述一律不通过。
- **R11.2 风险 —— 达成后立刻推新目标**：这是同类产品的惯性设计，与本产品「不鼓励极端节食」的硬性边界冲突。
  **规避**：默认停在维持态，新目标仅由用户主动触发（AC-11.2.6）。

---

## 5. 热量计算引擎字段契约（供架构师/工程师直接消费）

### 5.1 输入 `CalorieInput`

| 字段 | 类型 | 必填 | 说明 / 取值 |
| --- | --- | --- | --- |
| `gender` | `"male" \| "female"` | 是 | 用于 BMR 公式与安全下限 |
| `age` | `number` | 是 | 周岁，来自出生日期；校验 14–100 |
| `heightCm` | `number` | 是 | 身高（cm） |
| `weightKg` | `number` | 是 | 当前体重（kg） |
| `targetWeightKg` | `number` | 是 | 目标体重（kg） |
| `targetWeeks` | `number` | 是 | 目标期限（周），> 0 |
| `activityLevel` | `"sedentary" \| "light" \| "moderate" \| "high" \| "athlete"` | 是 | 对应系数 1.2 / 1.375 / 1.55 / 1.725 / 1.9 |
| `macroRatio?` | `{ protein: number; fat: number; carb: number }` | 否 | 百分比，默认 `{25,25,50}`，三者之和须 = 100 |
| `weeklyLossKg?` | `number` | 否 | 每周目标减重（kg）。未给则由 `weightKg/targetWeightKg/targetWeeks` 推导 |

### 5.2 计算规则

| 步骤 | 公式 |
| --- | --- |
| BMR（男） | `10 × weightKg + 6.25 × heightCm − 5 × age + 5` |
| BMR（女） | `10 × weightKg + 6.25 × heightCm − 5 × age − 161` |
| TDEE | `BMR × activityFactor` |
| 目标缺口 raw | `weeklyLossKg × 7700 ÷ 7` |
| 缺口上限 cap | `TDEE × 0.30` |
| 校验与告警分层 | **硬错误（阻断计算）**：`weeklyLossKg` 非有限值或 ≤ 0、枚举非法、数值越界、`macroRatio` 之和 ≠ 100；**非阻断告警（照常计算 + 温和提示）**：`weeklyLossKg > 体重 × 2%`（详细契约见 ARCHITECTURE §4.5） |
| 有效缺口 | `min(rawDeficit, cap)` |
| 建议摄入 raw | `TDEE − 有效缺口` |
| 安全下限 floor | `female → 1200`，`male → 1500` |
| 建议摄入 final | `max(rawIntake, floor)` |
| 宏量营养素 | 蛋白 = `final × protein% ÷ 4`g；脂肪 = `final × fat% ÷ 9`g；碳水 = `final × carb% ÷ 4`g |

### 5.3 输出 `CalorieResult`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `bmr` | `number` | 四舍五入到整数 kcal |
| `tdee` | `number` | 整数 kcal |
| `targetDeficitRaw` | `number` | 未截断的原始缺口 |
| `deficitCap` | `number` | TDEE × 30% |
| `effectiveDeficit` | `number` | 实际采用的缺口（可能被截断） |
| `isDeficitCapped` | `boolean` | 是否触发 30% 上限截断 |
| `intakeRecommended` | `number` | 最终建议摄入 |
| `floorApplied` | `boolean` | 是否触发安全下限 |
| `safetyFloor` | `number` | 1200 / 1500 |
| `safetyMessages` | `string[]` | 面向用户的安全提示文案（见 §8） |
| `macros` | `{ proteinG: number; fatG: number; carbG: number }` | 克数 |
| `weeklyLossEffectiveKg` | `number` | 按最终摄入反推的每周实际减重 |
| `etaWeeks` | `number \| null` | 按有效缺口预测达成目标所需周数 |

**边界与顺序约定（必须实现且必须测试）**：先算 `effectiveDeficit = min(raw, cap)`，再算 `intake = max(tdee − effectiveDeficit, floor)`；**下限的优先级高于缺口上限**。两个标志位可同时为 `true`。

---

## 6. 页面结构与交互要点（12 组路由）

| # | 路由 | 页面 | 关键交互要点 |
| --- | --- | --- | --- |
| 1 | `/` | 落地页 | 产品介绍 + **免注册**热量计算器 + 隐私承诺 + 免责声明 |
| 2 | `/onboarding` | 首次引导 | 首屏免责声明确认 → 问卷（性别/出生日期/身高/当前体重/目标体重/目标期限/活动量/饮食偏好/常见疾病）→ 结果页展示预算 |
| 3 | `/dashboard` | 今日看板 | 今日剩余热量**大数字**、进度环（颜色渐变）、饮水组件、迷你趋势图、一句鼓励语 |
| 4 | `/diary` | 饮食日记 | 按餐分组（早/午/晚/加餐），每餐独立预算；餐间可拖拽；3 次点击完成记录；实时反馈本餐热量与今日进度环 |
| 5 | `/exercise` | 运动 | MET 表选择 + 时长输入 → 显示消耗并计入当日余额 |
| 6 | `/weight` | 体重 | 每日记录（建议早晨空腹）+ 备注；趋势图叠加 7 日移动平均线；波动正向文案 |
| 7 | `/habits` | 习惯 | 自定义习惯项打卡；显示当前连续/历史最佳；断签不惩罚 |
| 8 | `/tools` | 生活化工具 | 外卖换算器、零食救赎、聚餐模式、饮品计算器（分标签页/卡片） |
| 9 | `/fasting` | 断食计时器 | 默认关闭；开启时弹内容警告；首页显示剩余时间；「这不适合我」一级按钮 |
| 10 | `/report` | 周报 | 周维度摄入/缺口/体重/习惯汇总；微量营养素面板入口 |
| 11 | `/ai` | AI 助手 | 每日总结、今日方案、自由提问；显示剩余次数（每日 50 次）；AI 未配置时明确提示不可用 |
| 12 | `/profile` + `/settings/data` | 我的 / 数据管理 | 基础数据修改并自动重算；参考来源页；数据导出/导入；账号硬删除 |

**通用交互约束**
- 移动端优先，核心记录操作 ≤3 次点击；数字大、进度直观（进度环 + 颜色渐变）。
- 全局 kcal / kJ 切换（一期可先隐藏，二期开放）。
- 深色模式；WCAG AA 对比度；核心流程支持屏幕阅读器。
- 不主动发用户未开启的通知。

---

## 7. 文案语气规范（硬性）

**总原则**：鼓励式、不贩卖焦虑、无负罪感。禁止「失败」「超标警告」式红色恐吓文案；体重上涨显示「波动很正常，看趋势就好」；不主动发用户未开启的通知。

### 7.1 正 / 反例句对照表

| 场景 | ❌ 禁止（反例） | ✅ 采用（正例） |
| --- | --- | --- |
| 热量超出预算 | 「警告：今日超标 320 kcal！」 | 「今天吃得丰富一些，明天照常就好」 |
| 体重上涨 | 「体重上涨 0.8kg，请反思饮食」 | 「波动很正常，看趋势就好」 |
| 断签后 | 「你已经 5 天没记录，坚持失败」 | 「休息一下没关系，随时回来」 |
| 凌晨未记录 | 「今天还没记录，快去补上！」 | （不主动提醒，仅在用户进入时展示当日状态） |
| 触达安全下限 | 「摄入过低，注意健康」 | 「这已接近安全下限，建议把目标调得更温和一些」 |
| 触发 30% 缺口上限 | 「目标过于激进，已被拒绝」 | 「为了更可持续，已帮你把目标调整为更温和的节奏」 |
| 连续打卡 | 「连续打卡 30 天，不能断！」 | 「连续记录 30 天，很棒；偶尔休息也没关系」 |
| 聚餐后 | 「火锅后请立即运动两小时」 | 「聚餐很开心，接下来几天照常记录就行」 |
| 断食开启前 | 「开始断食，坚持就是胜利！」 | 「断食不适合所有人，了解风险后再决定是否开启」 |
| 零食提示 | 「一包薯片毁掉你今天的努力」 | 「一包薯片 ≈ 快走 70 分钟，也可以看看更低卡的替代」 |
| 疾病/用药意图（AI） | 给出诊断或停药建议 | 「这涉及医疗问题，建议咨询专业医师」 |
| 鼓励语 | 「你太胖了，必须努力」 | 「今天也照顾好自己」 |

---

## 8. 可测试用例（≥30 条）

> 编号规则：`TC-xx`。**优先级**列中 P0 = 一期必测。

### 8.1 热量引擎（含 4 条边界）

| ID | 用例 | 预期结果 | 优先级 |
| --- | --- | --- | --- |
| TC-01 | **验收用例 1**：女 30 岁 165cm 60kg，久坐，每周减 0.5kg | BMR = 1320；TDEE = 1584；缺口 = 550/天；建议摄入 = 1200 并提示**已达安全下限** | P0 |
| TC-02 | **验收用例 2**：女 30 岁 165cm 60kg，久坐（sedentary），**显式给定 `weeklyLossKg = 2`**（≈60kg 的 3.3%/周，属激进目标） | **该输入被接受：校验通过、不报错**（`weeklyLossKg = 2` 本身是合法输入）。计算：BMR = 1320.25 → TDEE = 1584.3；`targetDeficitRaw = 2200.0`；`deficitCap = 475.3`（TDEE × 0.30，保留 1 位小数）；`effectiveDeficit = 475.3`；`isDeficitCapped = true`；`intakeRecommended = 1200`；`floorApplied = true`。同时给出**两条鼓励式提示**：① 温和节奏「为了更可持续，已帮你把目标调整为更温和的节奏」；② 接近安全下限「这已接近安全下限，建议把目标调得更温和一些」。文案须遵守 §7 语气规范，**不得出现「失败」「超标警告」** | P0 |
| TC-03 | **验收用例 3**：扫描条形码 X，命中所收录包装食品 | 命中后返回每 100g 营养（热量/蛋白/脂肪/碳水）并写入当日记录 | P1（三期扫码为二期/三期，见待确认 Q6） |
| TC-04 | **边界-极低体重**：女，weightKg = 35，其余正常 | BMR 按公式计算不报错；建议摄入被 1200 下限钳制；`floorApplied = true` | P0 |
| TC-05 | **边界-大年龄**：男，age = 90，weightKg = 60，身高 170 | BMR 正确计算（负值参数不越界）；结果为正；不抛异常 | P0 |
| TC-06 | **边界-下限触发**：男 40 岁 175cm 70kg 久坐，每周减 0.45kg | BMR = 1598.75；TDEE = 1918.5；缺口 = 495（未超 30% 上限 575.55）；建议摄入 = 1423.5 被 1500 下限钳制为 **1500**；`floorApplied = true` 且 `isDeficitCapped = false` | P0 |
| TC-07 | **边界-30% 缺口上限**：构造 raw > TDEE×0.3 的任意输入 | `effectiveDeficit = cap`，`isDeficitCapped = true`，`intakeRecommended = TDEE − cap`（若仍低于下限则再取下限） | P0 |
| TC-08 | 同时触发上限与下限 | `isDeficitCapped = true` 且 `floorApplied = true`；`intakeRecommended = floor` | P0 |
| TC-09 | 活动系数映射 | sedentary=1.2、light=1.375、moderate=1.55、high=1.725、athlete=1.9 全部正确 | P0 |
| TC-10 | 宏量营养素默认比例 | 建议摄入 1200 时：蛋白 75g、脂肪 33.3g、碳水 150g（按 4/9/4 换算，四舍五入策略见 Q4） | P0 |
| TC-11 | 自定义宏量比例 | 传入 30/30/40 且和为 100 时正确换算；和 ≠ 100 时返回校验错误 | P0 |
| TC-12 | 修改基础数据触发重算 | 修改体重后 BMR/TDEE/缺口/建议摄入/宏量目标全部更新且与手算一致 | P0 |
| TC-13 | 预测曲线 | 目标达成预测曲线依 `etaWeeks` 生成；实际体重曲线按记录日期绘制 | P0 |
| TC-14 | 参考来源可溯源 | `/profile` 参考来源页列出 Mifflin-St Jeor 1990、WHO BMI 标准、2024 成人活动 MET 汇编 | P0 |
| TC-15 | taper 收窄缺口 | 体重接近目标时缺口逐步收窄，不出现突然骤降；不产生反弹式目标 | P1 |

> **TC-02 设计意图：为什么不硬校验拒绝？** ① 符合「无负罪感设计」原则——报错拒绝会让用户觉得被指责，与产品理念冲突；② TDEE 30% 上限 + 安全下限已构成**双重有效防线**，输入再激进也只会被钳到安全区间，无需额外拒绝；③ 温和纠偏比直接拒绝更可能让用户留下。因此 `weeklyLossKg > 体重 × 2%` 应作为**非阻断告警**处理：照常计算 + 温和提示，而非返回错误。（详细契约见 ARCHITECTURE §4.5）

### 8.2 四种记录方式

| ID | 用例 | 预期结果 | 优先级 |
| --- | --- | --- | --- |
| TC-16 | 方式①搜索记录：输入「番茄」模糊搜索并选中 | 命中中式食物库条目，展示每 100g 营养与常见份量 | P0 |
| TC-17 | 方式①份量换算：选「1 碗」且自定义克数 300g | 按 `每100g营养 × 300/100` 计算并写入记录 | P0 |
| TC-18 | 方式①收藏与「最近吃过」一键复记 | 收藏项与最近记录可一键复用，1 次点击完成记录 | P0 |
| TC-19 | 方式②快速加卡：仅输入名称 + 热量 | 记录成功入库，蛋白/脂肪/碳水允许为空且不阻塞 | P0 |
| TC-20 | 方式③条形码扫描命中 Open Food Facts | 返回每 100g 营养并写入当日记录（对应 TC-03） | P1 |
| TC-21 | 方式③条形码未命中 | 提示未收录并提供「快速加卡」入口，不产生空记录 | P1 |
| TC-22 | 方式④AI 识别：仅返回名称与数量 | 热量数值来自食物库匹配，AI 不直接给出热量数字 | P2 |
| TC-23 | 方式④AI 结果需确认 | AI 结果填表后未点击确认为不入库；确认后入库 | P2 |
| TC-24 | 方式④AI 未配置 | AI 入口提示不可用，其余记录方式与全部功能正常 | P2 |
| TC-25 | 记录流程 ≤3 次点击 | 从 `/diary` 到记录完成 ≤3 次点击，记录后显示本餐热量与今日进度环 | P0 |
| TC-26 | 套餐模板一键添加 | 保存的一餐模板可一键添加，热量合计正确 | P0 |
| TC-27 | 餐间拖拽调整 | 记录可在早/午/晚/加餐之间拖动，两餐热量预算同步更新 | P0 |

### 8.3 生活化工具与追踪

| ID | 用例 | 预期结果 | 优先级 |
| --- | --- | --- | --- |
| TC-28 | 外卖换算器：选择「麻辣烫 + 中份」 | 输出热量估算区间与「这样点更健康」替换建议 | P1 |
| TC-29 | 零食救赎：选择「一包薯片」 | 输出低卡替代 + 「≈ 快走 70 分钟」，文案不含恐吓措辞 | P1 |
| TC-30 | 聚餐模式：选择「火锅」 | 输出估算与当天其余餐次调整建议，无「超标警告」红色文案 | P1 |
| TC-31 | 饮品计算器：选择「珍珠奶茶 大杯全糖」 | 输出热量与低卡点单攻略（如减糖/换奶） | P1 |
| TC-32 | 运动消耗：MET 5.0、体重 60kg、时长 1h | 消耗 = 300 kcal，并计入当日热量平衡 | P1 |
| TC-33 | 饮水：一键 +250ml 后撤销 | 水量正确增加 250ml；撤销后恢复上一条状态 | P1 |
| TC-34 | 体重趋势与 7 日移动平均 | 趋势图叠加 7 日移动平均线；体重上涨时显示「波动很正常，看趋势就好」 | P0 |
| TC-35 | 无负罪感 streak 断签 | 不发送惩罚性提醒；用户回访文案为「休息一下没关系，随时回来」 | P1 |
| TC-36 | 断食首次开启 | 弹出内容警告，含饮食障碍求助资源链接，「这不适合我」为一级按钮；不主动推送 | P1 |
| TC-37 | 微量营养素面板 | 日/周视图展示 10 项指标与可选 DRI 参考条，且为免费功能 | P1 |

### 8.4 安全、隐私与鉴权

| ID | 用例 | 预期结果 | 优先级 |
| --- | --- | --- | --- |
| TC-38 | 免责声明与高风险人群提示 | 引导首屏强制确认免责声明；勾选孕期/哺乳期/疾病治疗期时给出醒目安全提示并建议就医 | P0 |
| TC-39 | 数据导出 | 导出 JSON（完整可恢复）+ CSV（体重等），内容包含体重/饮食/运动/习惯/饮水/设置 | P1 |
| TC-40 | 数据导入 | JSON 备份可完整恢复；CSV 模板可批量导入历史体重，格式错误给出明确报错 | P1 |
| TC-41 | 账号删除（硬删除） | 调用删除后数据库中该用户及关联数据被真实移除，非软删除标记 | P1 |
| TC-42 | **JWT 越权防护** | 客户端提交他人 `userId` 时，服务端忽略该字段，一律以 JWT 中的 userId 为准；请求他人资源返回 403/404 | P0 |
| TC-43 | 未携带/伪造 JWT | 受保护接口返回 401，不泄露任何数据 | P0 |
| TC-44 | AI 安全边界 | 命中疾病诊断/停药/极端节食意图时固定回复就医建议 | P0 |
| TC-45 | AI 限额 | 第 51 次请求返回限额提示；AI key 仅存在于服务端环境变量，前端产物中不出现 | P2 |
| TC-46 | 隐私承诺展示 | `/` 页展示「不出售数据、无广告 SDK、无第三方行为分析」；网络请求中不存在第三方行为分析 SDK | P0 |
| TC-47 | PWA 离线 | 断网后仍可查看历史记录与查看当日记录（写操作按离线队列策略，见 Q7） | P1 |
| TC-48 | 深色模式与无障碍 | 深色模式全页面生效；核心流程卡片可被屏幕阅读器正确朗读；对比度满足 WCAG AA | P1 |
| TC-49 | kcal/kJ 全局切换 | 切换后全站单位一致变化，数值换算正确（1 kcal = 4.184 kJ） | P1 |
| TC-50 | 性能 | 首屏可交互 < 2s（移动端 4G）；记录接口 P95 < 300ms | P0 |

> 合计 **50 条**用例，覆盖：热量引擎 4 条边界（TC-04~TC-08）+ 四大记录方式（TC-16~TC-24）+ 无负罪感文案（TC-29、TC-34、TC-35）+ 隐私导出/删除（TC-39~TC-41）+ JWT 越权防护（TC-42、TC-43）。

---

## 9. 数据格式契约（数据主权）

### 9.1 导出 JSON 顶层结构

```json
{
  "meta": { "app": "qingshenghuo", "schemaVersion": 1, "exportedAt": "ISO8601" },
  "profile": { "gender": "female", "birthDate": "1995-01-01", "heightCm": 165, "activityLevel": "sedentary", "dietaryPreference": [], "conditions": [] },
  "goals": { "targetWeightKg": 55, "targetWeeks": 12, "macroRatio": { "protein": 25, "fat": 25, "carb": 50 } },
  "weights": [{ "date": "2026-09-01", "weightKg": 60.0, "note": "" }],
  "meals": [{ "date": "2026-09-01", "mealType": "lunch", "items": [{ "name": "番茄炒蛋", "grams": 300, "kcal": 180, "proteinG": 8, "fatG": 12, "carbG": 8 }] }],
  "exercises": [{ "date": "2026-09-01", "type": "walking", "met": 3.5, "minutes": 40, "kcalBurned": 140 }],
  "habits": [{ "date": "2026-09-01", "habitId": "water", "done": true }],
  "water": [{ "date": "2026-09-01", "ml": 1500 }],
  "settings": { "unit": "kcal", "darkMode": false, "fastingEnabled": false }
}
```

### 9.2 导出 CSV

- 每个实体一个文件，统一使用 UTF-8 with BOM，首行为表头，日期为 `YYYY-MM-DD`。
- 文件清单：`weights.csv`、`meals.csv`、`exercises.csv`、`habits.csv`、`water.csv`。

### 9.3 CSV 导入模板（历史体重）

| 列名 | 必填 | 说明 |
| --- | --- | --- |
| `date` | 是 | `YYYY-MM-DD` |
| `weightKg` | 是 | 数字，0 < w < 500 |
| `note` | 否 | 文本，≤ 200 字 |

导入行为：同日期以导入值覆盖（提示条数）；非法行跳过并汇总报错，不影响合法行导入。

---

## 10. 交付分期

| 分期 | 范围 |
| --- | --- |
| **MVP（一期）** | 用户系统 + 热量引擎 + 饮食记录（搜索/快加/套餐）+ 体重追踪 + 看板 |
| **二期** | 条形码扫描、生活化工具集、习惯打卡、饮水、周报、微量营养素面板、断食计时器 |
| **三期** | AI 识别与 AI 助手、数据导入导出增强、PWA 推送、分享卡片 |
| **四期** | 自用场景增强：平台期说明（R11.1）、目标达成后的维持模式（R11.2，含进度环与 ETA 维持态） |

---

## 11. 待确认问题清单

| # | 问题 | 建议默认值（未确认前按此执行） |
| --- | --- | --- |
| Q1 | 微信扫码登录是否一期就需要？ | 否，按三期 P2 处理；一期仅邮箱验证码 |
| Q2 | 注册邮箱验证码是否接入第三方邮件服务？生产环境必须配置，开发环境用控制台/本地邮件池 | 开发环境打印验证码到服务端日志 |
| Q3 | BMR/TDEE 四舍五入策略：各步取整还是仅最终取整？ | **仅最终输出取整**，中间链路保留浮点；验收用例数值按此复现 |
| Q4 | 宏量营养素克数取整策略？ | 克数取整到 0.1g 展示，比较时容差 ±0.5g |
| Q5 | 目标期限与目标体重同时存在时如何反推每周减重？ | `weeklyLossKg = (weightKg − targetWeightKg) / targetWeeks`，并受 30% 上限与安全下限约束 |
| Q6 | 条形码扫描客户验收用例（TC-03）被列为验收项，但需求分期标注为二期——是否需要提前到一期？ | 默认按二期实现，但在 PRD 中保留为验收用例；如客户认为必须一期交付请确认 |
| Q7 | PWA 离线写入策略：离线时是否允许记录？ | 允许，使用本地队列，恢复联网后自动同步；冲突以本地最新时间为准 |
| Q8 | 食物库 ≥500 条的数据来源与版权？ | 需客户提供或采购合规数据源；本次不虚构数据 |
| Q9 | AI 服务商选型与调用成本由谁承担？ | 默认使用服务端环境变量配置的可替换 LLM；未配置时功能降级 |
| Q10 | 微量营养素 DRI 参考值采用哪一版标准（中国居民膳食营养素参考摄入量 vs WHO）？ | 默认采用中国居民膳食营养素参考摄入量（最新版），并在参考来源页标注 |
| Q11 | 运动 MET 表是否需区分强度档位（如快走/慢走）？ | 默认区分，具体档位待食物/运动数据集确定后补充 |
| Q12 | 「常见疾病」字段是否属于敏感健康信息，需要额外加密存储？ | 默认按敏感字段处理：加密存储、不进入导出的公开分享卡片 |
| Q13 | 多语言（繁体/英文）是否在规划内？ | 否，本期仅简体中文 |
| Q14 | 分享卡片（三期）是否会泄露体重等敏感数据？ | 默认仅分享非敏感内容（如习惯天数、鼓励语） |
| Q15 | R11.1 平台期判定阈值（连续天数 / 移动平均变化阈值）与 R11.2 维持模式容差取值？ | 默认「连续 21 天且 7 日移动平均变化绝对值 < 0.1 kg」判定平台期；维持模式容差 **±0.2 kg**；两者均需在 `/why-numbers` 标注 |
| Q16 | R11.2 进入维持模式后，R2.6「目标达成预测曲线 vs 实际体重曲线」如何呈现？ | 默认**保留实际体重曲线、隐藏预测线**（已无缺口可预测），并在 `/why-numbers` 说明隐藏原因 |

---

## 附：文档使用说明

- 本 PRD 采用**简单 PRD** 结构，未包含竞品分析章节（客户未要求）。
- 热量引擎的字段契约（§5）与用例（TC-01~TC-15）可直接被架构师与工程师消费，验收用例 1/2/3 原样收录。
- 所有热量/营养数值必须来自可溯源公式或食物库，**不允许 AI 直接估算热量**。
